<?php
//define('DS', DIRECTORY_SEPARATOR);
//define('ROOT', dirname(dirname(__FILE__)));  // Correctly set the root directory of your project
//
//// Get the URL from the query parameter, sanitize it and handle cases where it's not set
//$url = isset($_GET['url']) ? $_GET['url'] : '';  // Ensure the URL parameter is set and handle if it's not
//
//// Include the bootstrap file which usually contains the initial setup for your application
//require_once(ROOT . DS . 'library' . DS . 'bootstrap.php');
